import { StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#3fff79ff',
    alignItems: 'center',
    justifyContent:  'center'
  },

  containerInput:{
    marginTop: 50,
    flexDirection: 'column',
    alignItems: 'center',
    marginBottom: 10,
  },

  containerButton:{
  marginTop: 50,
  }
});

export default styles